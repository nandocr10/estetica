import { PercentageFormatPipePipe } from './percentage-format.pipe';

describe('PercentageFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
